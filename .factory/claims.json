[
  {
    "id": "demo-isolated",
    "claim": "Demo — sample data, nothing is saved",
    "where": "Demo banner and landing action",
    "test": "npm run test:browser -- --grep @claim:demo-isolated",
    "sandbox": "Fresh /demo context asserts the demo-only session key, Reset demo, and Start for real cleanup."
  },
  {
    "id": "sample-guests",
    "claim": "See a ready room with four sample guests",
    "where": "Landing action helper and /demo",
    "test": "npm run test:browser -- --grep @claim:sample-guests",
    "sandbox": "Fresh /demo context asserts exactly four named sample guests."
  },
  {
    "id": "demo-privacy",
    "claim": "The demo never contacts the room API or a third party",
    "where": "Demo banner, demo documentation, README",
    "test": "npm run test:browser -- --grep @claim:demo-privacy",
    "sandbox": "Fresh /demo context records every request and asserts same-origin static requests with no /api request."
  },
  {
    "id": "temporary-rooms",
    "claim": "Rooms expire after six hours",
    "where": "Landing facts, host board, Privacy page, README",
    "test": "cargo test --manifest-path server/Cargo.toml tests::claim_temporary_rooms_reject_expired_reads_joins_and_updates -- --exact",
    "sandbox": "Fresh SQLite room is advanced past expiry; its read, join, guest update, and host update all return the expired-room outcome."
  },
  {
    "id": "no-account-or-install",
    "claim": "No account. No install.",
    "where": "Landing facts, join flow, README",
    "test": "npm run test:browser -- --grep @claim:no-account-or-install",
    "sandbox": "Fresh context opens a real room without account fields, password fields, or an install manifest."
  },
  {
    "id": "offline-reload",
    "claim": "Demo works offline after the first visit",
    "where": "README and demo documentation",
    "test": "npm run test:browser -- --grep @claim:offline-reload",
    "sandbox": "Dedicated fresh context installs the worker, becomes controlled, goes offline, and reloads /demo from the cached shell."
  },
  {
    "id": "local-room-discovery",
    "claim": "Guests can find an opted-in room on the same network or enter its four-letter code",
    "where": "Landing setup, join screen, README",
    "test": "npm run test:browser -- --grep @claim:local-room-discovery",
    "sandbox": "Fresh host opens a discoverable room; a guest context finds it through network discovery and can still type a manual code."
  },
  {
    "id": "join-card",
    "claim": "The host gets a QR join card with TV and print views",
    "where": "Host board and README",
    "test": "npm run test:browser -- --grep @claim:join-card",
    "sandbox": "Fresh room asserts a rendered QR, activates TV presentation mode, and observes the print action invoke browser printing."
  },
  {
    "id": "guest-capacity",
    "claim": "A room supports up to 12 guests",
    "where": "README and room-full error",
    "test": "npm run test:browser -- --grep @claim:guest-capacity",
    "sandbox": "Fresh room receives 24 concurrent joins and asserts exactly 12 persist while every excess join returns 409."
  },
  {
    "id": "capability-checks",
    "claim": "Each guest checks the secure browser, local network, chosen input, and motion setting",
    "where": "Guest check and README",
    "test": "npm run test:browser -- --grep @claim:capability-checks",
    "sandbox": "Fresh keyboard guest reaches the check screen and observes each named measured result."
  },
  {
    "id": "input-practice",
    "claim": "Three keyboard inputs complete practice and reach the host",
    "where": "Guest practice and README",
    "test": "npm run test:browser -- --grep @claim:input-practice",
    "sandbox": "Fresh keyboard guest enters three accepted keys, sees Practice passed, and the room snapshot persists practice_ok."
  },
  {
    "id": "touch-authenticity",
    "claim": "Touch practice accepts touch input, not mouse clicks",
    "where": "Guest touch practice",
    "test": "npm run test:browser -- --grep @claim:touch-authenticity",
    "sandbox": "Fresh non-touch and touch contexts prove three mouse clicks stay at 0 of 3, while three touchscreen taps pass and persist the touch result."
  },
  {
    "id": "large-text",
    "claim": "The large-text control persists its readable text size",
    "where": "Header and README",
    "test": "npm run test:browser -- --grep @claim:large-text",
    "sandbox": "Fresh context enables large text, measures a 19px root size, reloads, and confirms the setting remains active."
  },
  {
    "id": "reduced-motion",
    "claim": "Reduced-motion preferences remove interface movement",
    "where": "Guest check, design documentation, README",
    "test": "npm run test:browser -- --grep @claim:reduced-motion",
    "sandbox": "Fresh reduced-motion context measures the primary action transition at zero or one microsecond."
  },
  {
    "id": "privacy-no-tracking",
    "claim": "Room Ready collects no contact data and uses no analytics, cookies, third-party scripts, fonts, or runtime services",
    "where": "Privacy page and README",
    "test": "npm run test:browser -- --grep @claim:privacy-no-tracking",
    "sandbox": "Fresh privacy flow records requests and cookies, asserts same-origin traffic only, and finds no email or telephone collection."
  },
  {
    "id": "game-fit-not-certification",
    "claim": "Room Ready compares the host-selected input mix and does not certify a specific game",
    "where": "Landing, host board, footer, Terms page, README",
    "test": "npm run test:browser -- --grep @claim:game-fit-not-certification",
    "sandbox": "Fresh host and keyboard guest show Fits setup; removing Keyboard from the host selection changes that guest to Not selected, and restoring it returns Fits setup."
  },
  {
    "id": "no-raw-network-address",
    "claim": "The room record does not store the raw network address",
    "where": "Privacy page",
    "test": "cargo test --manifest-path server/Cargo.toml tests::claim_no_raw_network_address_in_room_record -- --exact",
    "sandbox": "Fresh SQLite room uses a unique network address; its complete stored room record contains only the one-way 64-character match value, never the raw address."
  },
  {
    "id": "session-token-lifetime",
    "claim": "Host and guest tokens live in session storage and disappear when the browser session ends",
    "where": "Privacy page",
    "test": "npm run test:browser -- --grep @claim:session-token-lifetime",
    "sandbox": "Fresh host and guest browser sessions store their own tokens; closing both and opening a new session leaves no host or guest token and blocks host access."
  },
  {
    "id": "immediate-host-read",
    "claim": "A newly created room opens its host board",
    "where": "Primary real-room action",
    "test": "npm run test:browser -- --grep @claim:immediate-host-read",
    "sandbox": "Fresh browser creates a room while the first GET is forced to 404; retry recovers to the host board without showing the terminal missing-room state."
  }
]
